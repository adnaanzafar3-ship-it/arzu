"use client"; import {useEffect,useState} from "react"; import {api} from "../../lib/api";
export default function Exams(){const[x,setX]=useState<any[]>([]);useEffect(()=>{api<any[]>("/api/v1/exams").then(setX)},[]);return <div className="container py-12"><h1 className="text-4xl font-black">Examinations</h1><div className="grid md:grid-cols-2 gap-5 mt-8">{x.map(e=><div className="card" key={e.id}><h2 className="text-xl font-bold">{e.name}</h2><p className="text-slate-500">{e.conducting_authority}</p><div className="mt-4 grid gap-2"><p>Application deadline: {e.application_deadline}</p><p>Exam date: {e.exam_date}</p></div>{e.official_website&&<a target="_blank" href={e.official_website} className="text-teal-700 font-bold inline-block mt-4">Official website →</a>}</div>)}</div></div>}
