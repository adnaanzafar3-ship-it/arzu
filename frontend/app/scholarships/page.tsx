"use client"; import {useEffect,useState} from "react"; import {api} from "../../lib/api";
export default function Scholarships(){const[x,setX]=useState<any[]>([]);useEffect(()=>{api<any[]>("/api/v1/scholarships").then(setX)},[]);return <div className="container py-12"><h1 className="text-4xl font-black">Scholarship Finder</h1><div className="grid md:grid-cols-2 gap-5 mt-8">{x.map(s=><div className="card" key={s.id}><span className="badge">{s.kind}</span><h2 className="text-xl font-bold mt-3">{s.name}</h2><p className="text-slate-500">{s.provider}</p><p className="mt-3">{s.eligibility}</p><p className="mt-3 font-bold">{s.amount}</p>{s.application_url&&<a target="_blank" className="text-teal-700 font-bold inline-block mt-4" href={s.application_url}>Official application/source →</a>}</div>)}</div></div>}
