"use client";import{useState}from"react";import{api}from"../../lib/api";export default function AISearch(){const[q,setQ]=useState("");const[r,setR]=useState<any>();const[loading,setLoading]=useState(false);async function run(e:React.FormEvent){e.preventDefault();setLoading(true);try{setR(await api("/api/v1/ai/search",{method:"POST",body:JSON.stringify({question:q})}))}catch(e:any){setR({answer:e.message,results:[]})}finally{setLoading(false)}}return <div className="container py-12 max-w-6xl"><span className="badge">AI COLLEGE SEARCH</span><h1 className="text-5xl font-black mt-3">Search like you talk.</h1><p className="text-slate-500 mt-3">Try “private BHMS colleges in Karnataka with hostel” or “BAMS colleges near Bengaluru”.</p><form onSubmit={run} className="flex flex-col md:flex-row gap-3 mt-8"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="What are you looking for?" required/><button className="btn btn-electric">{loading?"Thinking…":"Search with AI ✦"}</button></form>{r&&<div className="grid lg:grid-cols-2 gap-6 mt-8"><div className="card"><h2 className="text-2xl font-black">AI analysis</h2><p className="whitespace-pre-wrap mt-4">{r.answer}</p></div><div className="card"><h2 className="text-2xl font-black">Matching records</h2><div className="grid gap-3 mt-4">{(r.results||[]).map((x:any)=><div className="border rounded-2xl p-4" key={x.id}><b>{x.name}</b><p className="text-slate-500">{x.city}, {x.state} • {x.type}</p><p className="text-sm mt-1">⭐ {x.rating} • Hostel: {x.hostel?"Yes":"No"} • Verified: {x.verified?"Yes":"Needs verification"}</p></div>)}</div></div></div>}</div>}