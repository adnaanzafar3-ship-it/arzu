
"use client";import{useEffect,useState}from"react";import Link from"next/link";import{api}from"../../lib/api";
export default function Colleges(){const[x,setX]=useState<any>({items:[]});const[q,setQ]=useState("");const[loading,setLoading]=useState(false);
async function load(){setLoading(true);try{setX(await api(`/api/v1/colleges?q=${encodeURIComponent(q)}&page=1&limit=24`))}finally{setLoading(false)}}useEffect(()=>{load()},[]);
return <div className="container py-12"><div className="max-w-3xl"><span className="badge">COLLEGE MODE</span><h1 className="text-5xl font-black mt-3">Find your people.</h1><p className="text-slate-500 mt-3 text-lg">Search the database or use <Link className="text-violet-700 font-bold" href="/ai-search">AI Search</Link> for natural language.</p></div><div className="flex gap-3 mt-8"><input className="input" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&load()} placeholder="College, city, state..."/><button className="btn btn-primary" onClick={load}>Search</button></div>{loading?<p className="mt-8">Loading...</p>:<div className="grid md:grid-cols-2 lg:grid-cols-3 gap-5 mt-8">{x.items.map((c:any)=><Link href={`/colleges/${c.id}`} className="card hover:-translate-y-1 transition" key={c.id}><div className="flex justify-between"><span className="badge">{c.college_type}</span><span>⭐ {c.rating?.toFixed?.(1)||"—"}</span></div><h2 className="text-xl font-black mt-4">{c.name}</h2><p className="text-slate-500">{c.city}, {c.state}</p><p className="mt-4 text-sm">{c.hostel?"🏠 Hostel":"—"} • {c.admission_status}</p>{c.verified&&<p className="text-xs font-bold text-emerald-600 mt-3">✓ Verified record</p>}</Link>)}</div>}</div>}
