"use client"; import {useState} from "react";import{api}from"../../lib/api";import{useRouter}from"next/navigation";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[msg,setMsg]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();try{const r=await api<any>("/api/v1/auth/login",{method:"POST",body:JSON.stringify({email,password})});localStorage.setItem("pn_token",r.access_token);localStorage.setItem("pn_role",r.role);router.push(r.role==="admin"?"/admin":"/dashboard")}catch(e:any){setMsg(e.message)}}return <div className="container py-16 max-w-md"><div className="card"><h1 className="text-3xl font-black">Login</h1><form className="grid gap-4 mt-6" onSubmit={submit}><input className="input" type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className="input" type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="btn btn-primary">Login</button></form>{msg&&<p className="text-red-600 mt-4">{msg}</p>}<p className="text-slate-500 text-sm mt-5">Demo admin: admin@padhaanewala.in / ChangeMe123!</p></div></div>}
