"use client"; import {useEffect,useState} from "react"; import Link from "next/link"; import {api} from "../../lib/api";
export default function MockTests(){const[x,setX]=useState<any[]>([]);useEffect(()=>{api<any[]>("/api/v1/mock-tests").then(setX)},[]);return <div className="container py-12"><h1 className="text-4xl font-black">Mock Tests</h1><div className="grid md:grid-cols-2 gap-5 mt-8">{x.map(t=><Link href={`/mock-tests/${t.id}`} className="card" key={t.id}><span className="badge">{t.difficulty}</span><h2 className="text-xl font-bold mt-3">{t.title}</h2><p>{t.subject} • {t.duration_minutes} minutes</p><p className="text-slate-500 mt-2">{t.description}</p></Link>)}</div></div>}
