"use client"; import {useEffect,useState} from "react";import {useParams} from "next/navigation";import{api}from "../../../lib/api";
export default function Test(){const p=useParams();const[x,setX]=useState<any>();const[answers,setAnswers]=useState<Record<number,string>>({});const[score,setScore]=useState<number|null>(null);useEffect(()=>{if(p.id)api(`/api/v1/mock-tests/${p.id}`).then(setX)},[p.id]);if(!x)return <div className="container py-12">Loading...</div>;function submit(){let s=0;x.questions.forEach((q:any)=>{if(answers[q.id]===q.answer)s++});setScore(s)}return <div className="container py-12 max-w-4xl"><div className="card"><h1 className="text-3xl font-black">{x.test.title}</h1>{x.questions.map((q:any,i:number)=><div className="mt-8" key={q.id}><p className="font-bold">{i+1}. {q.question}</p>{["A","B","C","D"].map(o=><label className="block mt-2" key={o}><input type="radio" name={`q${q.id}`} checked={answers[q.id]===o} onChange={()=>setAnswers({...answers,[q.id]:o})}/> <span className="ml-2">{o}. {(q as any)[`option_${o.toLowerCase()}`]}</span></label>)}</div>)}<button onClick={submit} className="btn btn-primary mt-8">Submit Test</button>{score!==null&&<div className="card mt-5 bg-teal-50"><h2 className="text-2xl font-bold">Score: {score}/{x.questions.length}</h2><p className="text-slate-600">Review solutions with your teacher/counsellor or use the question explanations in the test data.</p></div>}</div></div>}
