"use client";
import {useEffect,useState} from "react"; import {useParams} from "next/navigation"; import {api} from "../../../lib/api";
export default function CoursePage(){const p=useParams();const[c,setC]=useState<any>();useEffect(()=>{if(p.slug)api(`/api/v1/courses/${p.slug}`).then(setC)},[p.slug]);if(!c)return <div className="container py-12">Loading...</div>;return <div className="container py-12"><div className="card max-w-4xl"><span className="badge">{c.degree}</span><h1 className="text-4xl font-black mt-4">{c.name}</h1><div className="grid md:grid-cols-2 gap-5 mt-8"><div><b>Duration</b><p>{c.duration}</p></div><div><b>Entrance Exam</b><p>{c.entrance_exam}</p></div><div><b>Eligibility</b><p>{c.eligibility}</p></div><div><b>Fees</b><p>{c.fees||"Check institution"}</p></div></div><h2 className="text-2xl font-bold mt-8">Admission</h2><p className="text-slate-600 mt-2">{c.admission_procedure}</p><h2 className="text-2xl font-bold mt-8">Career</h2><p className="text-slate-600 mt-2">{c.career||"Career information will be added."}</p></div></div>}
