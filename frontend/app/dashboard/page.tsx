"use client";import{useEffect,useState}from"react";import{api}from"../../lib/api";import{useRouter}from"next/navigation";
export default function Dashboard(){const[u,setU]=useState<any>();const router=useRouter();useEffect(()=>{const t=localStorage.getItem("pn_token");if(!t){router.push("/login");return}api("/api/v1/auth/me",{headers:{Authorization:`Bearer ${t}`}}).then(setU).catch(()=>router.push("/login"))},[router]);return <div className="container py-12">{u&&<><h1 className="text-4xl font-black">Welcome, {u.name}</h1><div className="grid md:grid-cols-3 gap-5 mt-8"><div className="card"><b>Email</b><p>{u.email}</p></div><div className="card"><b>Role</b><p>{u.role}</p></div><div className="card"><b>Mobile</b><p>{u.mobile||"Not added"}</p></div></div></>}</div>}
